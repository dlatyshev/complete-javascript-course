// Number - used for decimals and integers
// String - user for characters sets
// Boolean - Logical type that can ony be true or false
// Undefined - Value taken by a variable that is not yet defined
// Null - Also means "emmpty value"
// Symbol - Value that is unique and cannot be changed
// BigInt - Larger integers then the Number type can hold

// typeof - special operator to check type of some variable or literal

console.log(typeof true);
console.log(typeof (10));
console.log(typeof (1.1));
console.log(typeof undefined);
console.log(typeof null); // this is a javascript bug. This expression returns 'object' as a type. But null is no an object.