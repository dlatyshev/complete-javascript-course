const calculateAge = function (birthYear, currentYear) {
    return currentYear - birthYear;
}

console.log(calculateAge(1989, 2024)); 

// The difference between expression and declaration is that we can call declared function before it was declared.